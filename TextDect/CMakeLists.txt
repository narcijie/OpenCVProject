cmake_minimum_required(VERSION 2.8)
project(TextDect)
find_package(OpenCV REQUIRED)
set(SOURCE_FILES textdetection.cpp)
add_executable(TextDect ${SOURCE_FILES})
target_link_libraries(TextDect ${OpenCV_LIBS})
